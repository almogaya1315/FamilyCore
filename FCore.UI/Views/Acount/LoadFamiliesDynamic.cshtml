@model ICollection<SelectListItem>

<div class="container" style="padding-left:0">
    <input type="text" id="relfam_droptext" style="display: block; line-height: inherit; height: 20px; margin-bottom: -1px" />
    @Html.DropDownList("RelativeFamily", Model, new { @id = "famenum_box" })
</div>

@*<script>
    $(function () {
        var relfam = $('#relfam_droptext');
        relfam.on('input', function () {
            var text = relfam.val();
            if (text >= 2) {
                $.ajax({
                    url: '/Acount/LoadFamiliesDynamic',
                    type: 'Get',
                    data: text,
                    cache: false,
                    contentType: false,
                    processData: false,
                    success: function () {
                        var relmem_text = $('#relmem_droptext');
                        relmem_text.prop('disabled', false);
                        var relmem = $('#memenum_box');
                        relmem.prop('disabled', false);
                        
                        var url = '@Url.Action("LoadFamiliesDynamic", "Acount")';
                        $('#famenum_div').load(url);
                    }
                })
            }
        })
    })


</script>*@